@*---------------------------------------------------MENU FOR ALL PAGES---------------------------------------------------*@

<nav class="navbar navbar-inverse">
    <div style="padding: 20px">
        @if (HttpContext.Current.User.Identity.IsAuthenticated)
        {
            <div style="color: white">
                @HttpContext.Current.User.Identity.Name
                @*@if (HttpContext.Current.User.IsInRole("admin"))
                    {
                        <b> У Вас есть права администратора </b>
                    }*@
                @{
            if (Session["IsAdminFlag"] != null)
            {
                if ((bool)Session["IsAdminFlag"])
                {
                    <b>
                        У вас есть права администратора
                    </b>
                }
            }
                }
            </div>
            <button type="button" class="btn btn-warning active" >
                @Html.ActionLink("Выход", "LogOff", "Account")
            </button>
            <button type="button" class="btn btn-warning active">
                @Html.ActionLink("Мои альбомы", "Me", "Profile")
            </button>
        }
        else
        {
            <button type="button" class="btn btn-warning active">
                @Html.ActionLink("Вход", "Login", "Account")
            </button>
            <button type="button" class="btn btn-warning active">
                @Html.ActionLink("Регистрация", "Register", "Account")
            </button>
        }
        <button type="button" class="btn btn-warning active">
            @Html.ActionLink("Все пользователи", "AllUsers", "Account")
        </button>
    </div>

</nav>